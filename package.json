{
  "name": "tip-securepass",
  "version": "1.0.0",
  "private": true,
  "description": "Academic password-reset security and usability demonstration",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "netlify build",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "netlify-cli": "27.4.2",
    "prettier": "3.9.6"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
